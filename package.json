{
  "name": "agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "start": "cd dreams && bun run src/index.ts",
    "build": "cd dreams && bun install"
  }
}

